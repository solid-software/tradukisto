name: tradukisto
description: Convert numbers to words (text representation).
version: 0.2.0
repository: https://github.com/solid-software/tradukisto

environment:
  sdk: ^3.4.3

# Add regular dependencies here.
dependencies:
  decimal: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
